{
  "name": "crm-eye",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --shell powershell.exe \"npm run dev:web\" \"npm run dev:worker\"",
    "dev:web": "npm run dev -w @crm-eye/web",
    "dev:worker": "npm run dev -w @crm-eye/worker",
    "build": "npm run build:packages && npm run build -w @crm-eye/worker && npm run build -w @crm-eye/web",
    "build:packages": "npm run build -w @crm-eye/database && npm run build -w @crm-eye/shared && npm run build -w @crm-eye/ai",
    "start:web": "npm run start -w @crm-eye/web",
    "start:worker": "npm run start -w @crm-eye/worker",
    "start:all": "sh scripts/start-all.sh",
    "lint": "npm run lint -w @crm-eye/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "next": {
      "postcss": "^8.5.25",
      "sharp": "^0.35.0"
    },
    "postcss": "^8.5.25",
    "sharp": "^0.35.0"
  }
}
